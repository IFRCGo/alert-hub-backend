services:
  web:
    image: $DOCKER_IMAGE_BACKEND
    environment:
      CI: "true"
      DJANGO_SECRET_KEY: "CI-INSECURE-DJANGO-SECRET-KEY"
    volumes:
      - ./:/code
      - ./ci-share/:/ci-share/
      # - ./coverage/:/code/coverage/

  worker: !reset null
  worker-beat: !reset null
